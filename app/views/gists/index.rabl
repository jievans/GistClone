collection @gists

extends('gists/show')



# attributes :id, :title
#
# node(:favorite) do |gist|
#   !(gist.favorites.where(:user_id => current_user.id).count == 0)
#   # JSON.parse(gist.favorites.where(:user_id => current_user.id).to_json)
# end



